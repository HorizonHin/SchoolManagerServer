<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.schoolmanagerserver.mapper.StudentCourseMapper">

    <!-- 可选课程列表（这里未加容量/时间筛选，可按需扩展） -->
    <select id="findAllSelectableCourses" resultType="com.schoolmanagerserver.pojos.CourseDto">
        SELECT c.id AS courseId,
               NULL AS username,
               c.name AS coursename,
               c.teacher_name AS teacher
        FROM course c
        ORDER BY c.id
    </select>

    <!-- 某学生已选课程 -->
    <select id="findSelectedCourses" parameterType="string" resultType="com.schoolmanagerserver.pojos.CourseDto">
        SELECT c.id AS courseId,
               sc.student_name AS username,
               c.name AS coursename,
               c.teacher_name AS teacher
        FROM student_course sc
        JOIN course c ON c.id = sc.course_id
        WHERE sc.student_name = #{studentName}
          AND sc.status = 0
        ORDER BY c.id
    </select>

    <!-- 删除学生所有已选记录（覆盖提交前调用） -->
    <delete id="deleteSelectionsByStudent" parameterType="string">
        DELETE FROM student_course WHERE student_name = #{studentName}
    </delete>

    <!-- 批量插入新的选课记录（当前示例固定学期，可扩展增加 semester 参数） -->
    <insert id="batchInsertSelections">
        INSERT INTO student_course (student_name, course_id, semester, status, select_time)
        VALUES
        <foreach collection="courseIds" item="cid" separator=",">
            (#{studentName}, #{cid}, '2024-2025-1', 0, NOW())
        </foreach>
    </insert>

</mapper>

