spring:
  application:
    name: SchoolManagerServer
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/schmng
    username: root  # 从环境变量读取用户名
    password: 244466666  # 从环境变量读取密码


  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 0
pagehelper:
  dialect: mysql

mybatis:
  type-aliases-package: com.schoolmanagerserver.pojos

#  一天的毫秒数
tokenExpireMs: 86400000
JWTKey: aaa

server:
  port: 8081
